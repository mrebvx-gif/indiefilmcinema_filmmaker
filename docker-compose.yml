services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: indiefilmcinema
    ports:
      - "5433:5432"
    volumes:
      - indiefilm_pgdata:/var/lib/postgresql/data

volumes:
  indiefilm_pgdata:
